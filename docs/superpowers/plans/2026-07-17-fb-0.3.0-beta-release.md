# FB 0.3.0-beta Plugin Rebuild and Release

## Summary

Rebuild FB as `0.3.0-beta`, reflecting the six-workstream model, updated BFM
loop, session/eval harness, efficiency controls, and simplified documentation.
The installed build is `0.3.0-beta+codex.<UTC-build>`.

## Task 1: Rebuild and verify the release candidate

### Candidate changes

- Regenerate every declared plugin mirror, including the full FB loop diagram.
- Update plugin metadata and default prompts for Product/User, Business, Design,
  Tech, Discovery, Bugs, ready handoffs, `$bfm`, automated testing and repair,
  Ready to ship, and Push Live.
- Update both plugin manifests, active version documentation, changelog,
  marketplace-facing copy, setup guidance, and current release records to
  `0.3.0-beta`. Preserve historical version evidence.
- Add a focused contract preventing manifest version mismatch, stale
  four-workstream prompts, missing workstreams, or missing release-boundary
  language.

### Verification and release boundary

- Run package generation/parity, plugin validation, focused root/package
  six-workstream, six-skill, positioning, metadata, syntax, link, and whitespace
  contracts.
- Run the complete repository validator once at the final release checkpoint.
- In an isolated temporary Codex home, install the candidate and prove its new
  version, six skills, six-workstream BFM guidance, bundled MCP resolution, and
  current diagrams/docs.
- Commit and push the candidate and report Ready to ship with GitHub review
  links. Do not merge to `main` until James says `Push Live`.
- After `Push Live`, merge, upgrade the GitHub marketplace, reinstall, and
  verify the public active build.

### Compatibility

Keep `fb-lane-coordination`, marketplace `fb-lane`, `$bfm`, CLI paths, MCP
names, and technical identifiers unchanged. Existing four-workstream records
remain valid; new and upgraded projects use six workstreams.
