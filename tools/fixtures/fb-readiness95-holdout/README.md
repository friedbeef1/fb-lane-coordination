# First-pass release candidate

Implement `buildCandidate(input)` in `src/release-candidate.cjs`.

Read every current document under `docs/`. They contain the Feature, Bugs,
Tech, and Design rules. `docs/history.md` is explicitly historical and cannot
override current documents.

Run the public test once after your first candidate. Do not repair a failed
candidate, access files outside this exercise, merge, publish, or deploy.
