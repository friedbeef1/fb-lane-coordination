# Release candidate exercise

Implement `buildCandidate(input)` in `src/release-candidate.cjs`.

The function consolidates product work into a candidate. Read the documents in
`docs/` because they contain current decisions, stale material, blockers,
technical boundaries, bug evidence, and design requirements.

Run `npm test`. Do not merge, publish, deploy, or access files outside this
exercise directory.
