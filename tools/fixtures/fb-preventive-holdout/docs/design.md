# Design acceptance

- `designReview` must contain `ariaLabel`, `focusVisible`, and
  `narrowViewportChecked`.
- Every field must be `true` for a review-ready candidate.
- Keep only review links beginning with `https://`.
- When all automated checks pass, `userInputNeeded` is `none`.
