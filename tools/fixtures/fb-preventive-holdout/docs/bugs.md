# Bug evidence

- A bug is actionable only with non-empty `reproduction` and `observable`.
- Critical reproducible bugs come before features.
- A bug missing either field remains blocked with reason, owner, and next
  action. Do not silently fix or select it.
